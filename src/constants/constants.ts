export enum IconNames {
  backIcon = "backIcon",
  closeIcon = "closeIcon",
  searchIcon = "searchIcon",
  logo = "logo",
}

export enum SearchUrls {
  search = "search",
  page = "page",
}

export const itemsPerPage = 10;
